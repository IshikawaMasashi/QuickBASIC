// https://docs.microsoft.com/ja-jp/dotnet/api/system.valuetype?view=net-5.0
export default abstract class ValueType {
  // コンストラクター
  // テーブル 1
  // ValueType()
  // ValueType クラスの新しいインスタンスを初期化します。
  // メソッド
  // テーブル 2
  // Equals(Object)
  // このインスタンスと指定したオブジェクトが等しいかどうかを示します。
  // GetHashCode()
  // このインスタンスのハッシュ コードを返します。
  // GetType()
  // 現在のインスタンスの Type を取得します。(継承元 Object)
  // MemberwiseClone()
  // 現在の Object の簡易コピーを作成します。(継承元 Object)
  // ToString()
  // このインスタンスの完全修飾型名を返します。
}
